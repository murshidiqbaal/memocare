import 'package:flutter/material.dart';

/// Official Emotional Palette for MemoCare (Memory & Recall Theme)
/// Used for reminiscence, memories, and emotional support sections.
class EmotionalColors {
  // Brand (Memory Theme)
  static const Color primary = Color(0xFF8E97FD); // Soft Lavender
  static const Color secondary = Color(0xFF6BCBCA); // Calm Teal
  static const Color background = Color(0xFFFAFBFF); // Cream White
  static const Color surface = Color(0xFFFFFFFF);

  // Semantic
  static const Color error = Color(0xFFFF6B6B);
  static const Color warning = Color(0xFFFFB26B);
  static const Color success = Color(0xFF6BCB77);

  // Text
  static const Color textPrimary = Color(0xFF2D3142);
  static const Color textSecondary = Color(0xFF7A8FA6);
}
