# 🚨 Emergency SOS System - Architecture Diagram

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         PATIENT SIDE                                │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────┐
    │      Patient Main Screen                     │
    │  ┌────────────────────────────────────────┐  │
    │  │                                        │  │
    │  │         Main Content Area              │  │
    │  │    (Home/Reminders/Memories/Profile)   │  │
    │  │                                        │  │
    │  └────────────────────────────────────────┘  │
    │                                              │
    │  ┌────────────────────────────────────────┐  │
    │  │   Curved Bottom Navigation Bar         │  │
    │  │                                        │  │
    │  │  Home  Reminders  [SOS]  Memories  Profile │
    │  │   🏠      🔔       🆘      📷       👤   │  │
    │  │                    ↑                   │  │
    │  │              Pulsing Red Button        │  │
    │  └────────────────────────────────────────┘  │
    └──────────────────────────────────────────────┘
                         │
                         │ User taps SOS
                         ↓
    ┌──────────────────────────────────────────────┐
    │      SOS Countdown Dialog (Full Screen)      │
    │  ┌────────────────────────────────────────┐  │
    │  │         ⚠️  (Pulsing Icon)             │  │
    │  │                                        │  │
    │  │        EMERGENCY ALERT                 │  │
    │  │                                        │  │
    │  │  Sending alert to your caregiver...    │  │
    │  │                                        │  │
    │  │          ┌─────────────┐               │  │
    │  │          │      5      │ ← Countdown   │  │
    │  │          └─────────────┘               │  │
    │  │                                        │  │
    │  │     ┌───────────────────────┐          │  │
    │  │     │       CANCEL          │          │  │
    │  │     └───────────────────────┘          │  │
    │  └────────────────────────────────────────┘  │
    └──────────────────────────────────────────────┘
                         │
                         │ Countdown reaches 0
                         ↓
    ┌──────────────────────────────────────────────┐
    │   EmergencySOSController (State Management)  │
    │                                              │
    │   States:                                    │
    │   • idle                                     │
    │   • countdown(5,4,3,2,1)                     │
    │   • sending                                  │
    │   • sent                                     │
    │   • cancelled                                │
    │   • error                                    │
    └──────────────────────────────────────────────┘
                         │
                         │ Calls repository
                         ↓
    ┌──────────────────────────────────────────────┐
    │   EmergencyAlertRepository                   │
    │                                              │
    │   • Get current location (3s timeout)        │
    │   • Insert alert to Supabase                 │
    │   • Return EmergencyAlert model              │
    └──────────────────────────────────────────────┘
                         │
                         ↓

┌─────────────────────────────────────────────────────────────────────┐
│                      SUPABASE DATABASE                              │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────┐
    │         emergency_alerts Table               │
    │                                              │
    │  INSERT:                                     │
    │  {                                           │
    │    patient_id: "uuid",                       │
    │    status: "sent",                           │
    │    latitude: 40.7128,                        │
    │    longitude: -74.0060,                      │
    │    created_at: "2026-02-15T19:30:00Z"        │
    │  }                                           │
    └──────────────────────────────────────────────┘
                         │
                         │ Trigger fires
                         ↓
    ┌──────────────────────────────────────────────┐
    │   assign_primary_caregiver() Trigger         │
    │                                              │
    │   • Find first linked caregiver              │
    │   • Set caregiver_id                         │
    │   • Fetch patient name/phone                 │
    │   • Update row before insert                 │
    └──────────────────────────────────────────────┘
                         │
                         │ Realtime publication
                         ↓
    ┌──────────────────────────────────────────────┐
    │      Supabase Realtime (WebSocket)           │
    │                                              │
    │   • Broadcasts INSERT event                  │
    │   • Filters by caregiver_id                  │
    │   • Sends to subscribed clients              │
    └──────────────────────────────────────────────┘
                         │
                         ↓

┌─────────────────────────────────────────────────────────────────────┐
│                       CAREGIVER SIDE                                │
└─────────────────────────────────────────────────────────────────────┘

    ┌──────────────────────────────────────────────┐
    │   linkedPatientsAlertsStreamProvider         │
    │                                              │
    │   • Subscribes to emergency_alerts stream    │
    │   • Filters by linked patient IDs            │
    │   • Auto-updates on new alerts               │
    └──────────────────────────────────────────────┘
                         │
                         │ Real-time update
                         ↓
    ┌──────────────────────────────────────────────┐
    │      Caregiver Alerts Screen                 │
    │  ┌────────────────────────────────────────┐  │
    │  │  ┌──────────────────────────────────┐  │  │
    │  │  │ 🆘  John Doe          ● (pulse)  │  │  │
    │  │  │     2 minutes ago                │  │  │
    │  │  ├──────────────────────────────────┤  │  │
    │  │  │ 📍 Location: 40.7128, -74.0060   │  │  │
    │  │  │ 📞 Phone: +1 234 567 8900        │  │  │
    │  │  │                                  │  │  │
    │  │  │ ┌──────────┐  ┌──────────┐       │  │  │
    │  │  │ │   CALL   │  │ RESOLVE  │       │  │  │
    │  │  │ └──────────┘  └──────────┘       │  │  │
    │  │  └──────────────────────────────────┘  │  │
    │  └────────────────────────────────────────┘  │
    └──────────────────────────────────────────────┘
                         │
                         │ Tap CALL
                         ↓
    ┌──────────────────────────────────────────────┐
    │         Phone Dialer (tel: URI)              │
    │                                              │
    │   Launches system phone app with number      │
    └──────────────────────────────────────────────┘

                         │ Tap RESOLVE
                         ↓
    ┌──────────────────────────────────────────────┐
    │   EmergencyAlertRepository.resolveAlert()    │
    │                                              │
    │   UPDATE emergency_alerts                    │
    │   SET status = 'resolved',                   │
    │       resolved_at = NOW()                    │
    │   WHERE id = alert_id                        │
    └──────────────────────────────────────────────┘
                         │
                         │ Realtime update
                         ↓
    ┌──────────────────────────────────────────────┐
    │      Alert removed from active list          │
    └──────────────────────────────────────────────┘
```

---

## State Machine Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│              EmergencySOSController State Machine               │
└─────────────────────────────────────────────────────────────────┘

                    ┌──────────┐
                    │   IDLE   │ ← Initial state
                    └──────────┘
                         │
                         │ startCountdown()
                         ↓
              ┌─────────────────────┐
              │   COUNTDOWN(5)      │
              └─────────────────────┘
                         │
                         │ 1 second delay
                         ↓
              ┌─────────────────────┐
              │   COUNTDOWN(4)      │
              └─────────────────────┘
                         │
                         │ 1 second delay
                         ↓
              ┌─────────────────────┐
              │   COUNTDOWN(3)      │
              └─────────────────────┘
                         │
                         │ 1 second delay
                         ↓
              ┌─────────────────────┐
              │   COUNTDOWN(2)      │
              └─────────────────────┘
                         │
                         │ 1 second delay
                         ↓
              ┌─────────────────────┐
              │   COUNTDOWN(1)      │
              └─────────────────────┘
                         │
                         │ 1 second delay
                         ↓
              ┌─────────────────────┐
              │     SENDING         │
              └─────────────────────┘
                         │
                    ┌────┴────┐
                    │         │
            Success │         │ Error
                    ↓         ↓
          ┌──────────┐   ┌──────────┐
          │   SENT   │   │  ERROR   │
          └──────────┘   └──────────┘
                │             │
                │ 2s delay    │ 3s delay
                ↓             ↓
          ┌──────────────────────┐
          │       IDLE           │
          └──────────────────────┘

    Cancel path (from any COUNTDOWN state):
    
              ┌─────────────────────┐
              │   COUNTDOWN(n)      │
              └─────────────────────┘
                         │
                         │ cancelCountdown()
                         ↓
              ┌─────────────────────┐
              │    CANCELLED        │
              └─────────────────────┘
                         │
                         │ 1s delay
                         ↓
              ┌─────────────────────┐
              │       IDLE          │
              └─────────────────────┘
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    Data Flow: Patient → Caregiver               │
└─────────────────────────────────────────────────────────────────┘

Patient App                Supabase                Caregiver App
    │                         │                         │
    │ 1. Tap SOS              │                         │
    ├────────────────────────▶│                         │
    │                         │                         │
    │ 2. Start countdown      │                         │
    │    (5 seconds)          │                         │
    │                         │                         │
    │ 3. Countdown = 0        │                         │
    │                         │                         │
    │ 4. Get location         │                         │
    │    (3s timeout)         │                         │
    │                         │                         │
    │ 5. INSERT alert         │                         │
    ├────────────────────────▶│                         │
    │                         │                         │
    │                         │ 6. Trigger fires        │
    │                         │    (assign caregiver)   │
    │                         │                         │
    │                         │ 7. Realtime broadcast   │
    │                         ├────────────────────────▶│
    │                         │                         │
    │                         │                         │ 8. Alert appears
    │                         │                         │    instantly
    │                         │                         │
    │                         │                         │ 9. Caregiver taps
    │                         │                         │    RESOLVE
    │                         │                         │
    │                         │ 10. UPDATE status       │
    │                         │◀────────────────────────┤
    │                         │                         │
    │                         │ 11. Realtime broadcast  │
    │                         ├────────────────────────▶│
    │                         │                         │
    │                         │                         │ 12. Alert removed
    │                         │                         │     from list
    │                         │                         │
```

---

## Component Hierarchy

```
PatientMainScreen
├── IndexedStack (manages tabs)
│   ├── PatientDashboardTab (index 0)
│   ├── ReminderListScreen (index 1)
│   ├── MemoriesScreen (index 2)
│   └── PatientProfileScreen (index 3)
└── CurvedBottomNavBar
    ├── _NavBarItem (Home)
    ├── _NavBarItem (Reminders)
    ├── _SOSButton ← Central emergency button
    │   └── AnimatedBuilder (pulsing animation)
    ├── _NavBarItem (Memories)
    └── _NavBarItem (Profile)

SOSCountdownDialog (shown on SOS tap)
├── Dialog.fullscreen
└── Column
    ├── _buildWarningIcon
    │   └── TweenAnimationBuilder (scale animation)
    ├── _buildStatusMessage
    ├── _buildCountdownDisplay
    │   └── TweenAnimationBuilder (elastic bounce)
    └── _buildActionButtons
        └── _buildCancelButton OR _buildCloseButton

CaregiverAlertsScreen
├── StreamProvider (linkedPatientsAlertsStreamProvider)
└── ListView.builder
    └── _EmergencyAlertCard (for each alert)
        ├── Header (patient name, time, pulsing indicator)
        ├── Content (location, phone)
        └── Actions
            ├── Call Button (launches tel: URI)
            └── Resolve Button (updates status)
```

---

## Database Schema Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                    Database Relationships                       │
└─────────────────────────────────────────────────────────────────┘

auth.users (Supabase Auth)
    │
    ├─────────────────────────────────────┐
    │                                     │
    ↓                                     ↓
profiles                          caregiver_profiles
(id, full_name, phone_number)     (id, user_id, phone, ...)
    │                                     │
    │                                     │
    │                                     ↓
    │                          caregiver_patient_links
    │                          (caregiver_id, patient_id)
    │                                     │
    │                                     │
    └─────────────┬───────────────────────┘
                  │
                  ↓
          emergency_alerts
          ┌─────────────────────────────────┐
          │ id (PK)                         │
          │ patient_id (FK → auth.users)    │
          │ caregiver_id (FK → auth.users)  │
          │ status (sent/cancelled/resolved)│
          │ created_at                      │
          │ resolved_at                     │
          │ latitude, longitude             │
          │ patient_name (denormalized)     │
          │ patient_phone (denormalized)    │
          └─────────────────────────────────┘

Trigger: assign_primary_caregiver()
  • Runs BEFORE INSERT on emergency_alerts
  • Finds first linked caregiver via caregiver_patient_links
  • Sets caregiver_id automatically
  • Fetches patient_name and patient_phone from profiles
```

---

## Security Model (RLS)

```
┌─────────────────────────────────────────────────────────────────┐
│              Row Level Security Policies                        │
└─────────────────────────────────────────────────────────────────┘

Policy 1: "Patients can create their own emergency alerts"
  Operation: INSERT
  Check: auth.uid() = patient_id
  
  ✅ Patient A can INSERT alert with patient_id = A
  ❌ Patient A cannot INSERT alert with patient_id = B

Policy 2: "Patients can view their own emergency alerts"
  Operation: SELECT
  Using: auth.uid() = patient_id
  
  ✅ Patient A can SELECT alerts where patient_id = A
  ❌ Patient A cannot SELECT alerts where patient_id = B

Policy 3: "Patients can cancel their own emergency alerts"
  Operation: UPDATE
  Using: auth.uid() = patient_id
  Check: auth.uid() = patient_id
  
  ✅ Patient A can UPDATE (cancel) alerts where patient_id = A
  ❌ Patient A cannot UPDATE alerts where patient_id = B

Policy 4: "Caregivers can view alerts from linked patients"
  Operation: SELECT
  Using: EXISTS (
    SELECT 1 FROM caregiver_patient_links cpl
    INNER JOIN caregiver_profiles cp ON cpl.caregiver_id = cp.id
    WHERE cp.user_id = auth.uid()
    AND cpl.patient_id = emergency_alerts.patient_id
  )
  
  ✅ Caregiver C can SELECT alerts from linked Patient A
  ❌ Caregiver C cannot SELECT alerts from unlinked Patient B

Policy 5: "Caregivers can resolve alerts from linked patients"
  Operation: UPDATE
  Using: (same EXISTS check as Policy 4)
  Check: (same EXISTS check as Policy 4)
  
  ✅ Caregiver C can UPDATE (resolve) alerts from linked Patient A
  ❌ Caregiver C cannot UPDATE alerts from unlinked Patient B
```

---

## Performance Optimizations

```
┌─────────────────────────────────────────────────────────────────┐
│                   Performance Strategy                          │
└─────────────────────────────────────────────────────────────────┘

Database Level:
  ✅ Indexes on patient_id, caregiver_id, status, created_at
  ✅ Denormalized patient_name and patient_phone (avoid joins)
  ✅ Trigger auto-assigns caregiver (single query)
  ✅ Composite unique index on (patient_id, created_at)

Repository Level:
  ✅ Location fetch with 3-second timeout (non-blocking)
  ✅ Graceful degradation (works without location)
  ✅ Error handling with try-catch
  ✅ Stream subscription (only active alerts)

Provider Level:
  ✅ autoDispose providers (automatic cleanup)
  ✅ Optimistic UI updates (instant feedback)
  ✅ State machine prevents race conditions
  ✅ Invalidation triggers refresh only when needed

UI Level:
  ✅ IndexedStack (keeps tab state alive)
  ✅ AnimationController disposal
  ✅ Conditional rendering (only show what's needed)
  ✅ Debounced button taps (prevent double-tap)
```

This visual architecture should help understand how all the pieces fit together!
